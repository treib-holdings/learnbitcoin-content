---
title: "BIP 341 (Taproot)"
slug: bip-341
draft: false
updated: "2026-09-17"
shortDefinition: "Defines Taproot - SegWit version 1 outputs that use a tweaked x-only public key directly as the address, with no hash layer in front of the key."
keyTakeaways:
  - "Defines the P2TR output type, where the bech32m address IS the tweaked public key"
  - "Two spending paths: cooperative key-path (single Schnorr signature) or script-path (Merkle proof of one branch)"
  - "Activated as a soft fork in November 2021 alongside BIP-340 (Schnorr) and BIP-342 (Tapscript)"
sources: []
relatedTerms:
  - bip-bitcoin-improvement-proposal
  - taproot
  - schnorr-signature
  - bip-342-tapscript
  - segwit-segregated-witness-bip-141
  - soft-fork
  - merkleized-abstract-syntax-tree-mast
  - p2wpkh-pay-witness-public-key-hash
  - p2wsh-pay-witness-script-hash
  - public-key
  - elliptic-curve
  - post-quantum-bitcoin
sameAs:
  - "https://github.com/bitcoin/bips/blob/master/bip-0341.mediawiki"
  - "https://en.bitcoin.it/wiki/BIP_0341"
  - "https://bitcoinops.org/en/topics/taproot/"
liveWidget: ~
---

[BIP-341](https://github.com/bitcoin/bips/blob/master/bip-0341.mediawiki) defines [Taproot](/glossary/taproot) - SegWit version 1 outputs that use a tweaked x-only [public key](/glossary/public-key) directly as the address. Activated as a [soft fork](/glossary/soft-fork) in November 2021 at block height 709,632, alongside [BIP-340](https://github.com/bitcoin/bips/blob/master/bip-0340.mediawiki) (Schnorr signatures) and [BIP-342](/glossary/bip-342-tapscript) (Tapscript).

The headline structural choice: **there is no hash layer in front of the public key.** The bech32m address (the part starting with `bc1p...`) IS a 32-byte tweaked x-only pubkey, presented directly to anyone reading the chain. Earlier hashed-address types (P2PKH, P2WPKH) hide the pubkey behind a 160-bit hash until the address is spent. P2TR does not. Every P2TR output puts its key on chain at creation.

This choice is what makes every P2TR output look identical on the wire until it is spent - the script tree commitment is folded into the tweak rather than appearing as a separate hash layer - and it is also why P2TR outputs are "always-exposed" in [post-quantum](/glossary/post-quantum-bitcoin) terms: the public key is forensically reachable from the moment the output is created, regardless of whether the output is ever spent.

## The two spending paths

A P2TR output commits to either a single public key (the "internal key"), a tree of alternative spending scripts (the "script paths"), or both. The taproot tweak combines them into a single output key that doesn't reveal which paths exist.

1. **Key-path spend.** A single [Schnorr signature](/glossary/schnorr-signature) over the tweaked key. The witness contains a single 64-byte signature (65 bytes if a non-default sighash type is appended). Observers cannot tell whether script paths existed or what they would have been. This is the common case and the cheapest spend type in Bitcoin.
2. **Script-path spend.** Reveals one leaf of a [Merkleized script tree](/glossary/merkleized-abstract-syntax-tree-mast), proves it was committed to in the tweak, and executes the leaf script under [Tapscript](/glossary/bip-342-tapscript). Other branches stay hidden. Pay for the branch you actually use.

Authored by Pieter Wuille, Jonas Nick, and Anthony Towns. The activation followed a three-month "Speedy Trial" signaling period, ending in lock-in in June 2021 and full activation in November.

Spec: [BIP-341](https://github.com/bitcoin/bips/blob/master/bip-0341.mediawiki).

See [How Taproot Actually Works](/rabbit-hole/how-taproot-works) for the construction with a diagram and what each spending path reveals.
